[phases.setup]
nixPkgs = ["python311", "gcc", "rustc", "cargo"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmd = "python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
